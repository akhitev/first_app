<%= form_for current_user.appreciations.build(:liked_id => micropost.id), :remote=> true do |f| %>
  <div><%= f.hidden_field :liked_id ,:value => micropost.id %></div>
  <div class="actions"><%= f.submit "Like" %></div>
<% end %>